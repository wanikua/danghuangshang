services:
  court:
    image: boluobobo/ai-court:latest
    build: .   # 本地构建备选（无预构建镜像时自动 build）
    container_name: ai-court
    restart: unless-stopped
    ports:
      - "127.0.0.1:18789:18789"   # Gateway Dashboard
      - "127.0.0.1:18795:18795"   # 菠萝 GUI（可选）
    volumes:
      # 配置持久化（init-court 生成的配置不会因容器重建丢失）
      - court-config:/home/court/.openclaw
      # 工作区持久化（记忆、日志等）
      - court-workspace:/home/court/clawd
      # OpenViking 数据持久化（可选）
      - court-openviking:/home/court/.openviking
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:18789/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s   # 等待配置初始化

volumes:
  court-config:
  court-workspace:
  court-openviking:
